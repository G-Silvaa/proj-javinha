version: '3.8'

services:

  db:
    image: postgres
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_USER: geniltonbd
      POSTGRES_PASSWORD: admin123

  adminer:
    image: adminer
    restart: always
    ports:
